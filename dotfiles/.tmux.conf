# Set space as leader
unbind Space
set -g prefix C-Space
bind C-Space send-prefix

# Base 1 for numbering
set -g base-index 1
setw -g pane-base-index 1
set-option -g renumber-windows on

# Neovim recommendations
set-option -sg escape-time 10
set-option -g focus-events on
